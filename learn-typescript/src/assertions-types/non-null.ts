/**
 * @description The non-null assertion operator (!) is a type assertion in TypeScript that 
 * allows you to tell the compiler that a value will never be null or undefined.
 */

// let name: string | null = null;

// we use the non-null assertion operator to tell the compiler that name will never be null
// let nameLength = name!.length;